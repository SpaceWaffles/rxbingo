<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="@dimen/element_spacing_medium_small">

    <io.spacenoodles.rxbingo.view.BingoView
        android:id="@+id/bingo_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:grid_color="@color/grid_color"
        app:title_color="@color/colorPrimary"
        app:number_color="@color/grid_color"
        app:name_color="@color/grid_color"
        app:marked_square_color="@color/marked_square_color"/>

</FrameLayout>